fileVersion: 1
id: "1"
macroString: |
  {%- macro rename_column(col) -%}
  {{ 
      parameters.columns
      | selectattr('originalName', 'equalto', col)  
      | map(attribute='transName')                
      | first                                      
  }}
  {%- endmacro -%}
name: macro
type: Macro
